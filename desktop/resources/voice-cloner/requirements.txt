# Everything clone.py needs. Torch is the big one (~2 GB); if you have a CUDA
# or Intel GPU, install the matching torch build first and this file will leave
# it alone.
torch>=2.4
torchaudio>=2.4
onnx>=1.16
onnx2torch>=1.5
onnxruntime>=1.18
soundfile>=0.12
# WavLM (the default loss). --loss speaker runs without it.
transformers>=4.40
numpy>=1.26
