/// <reference types="vite/client" />

/** Baked at build time (electron.vite.config.ts): the beta build's deadline
 * (MONET_BETA_EXPIRES). Empty string on normal, unlimited builds. */
declare const __BETA_EXPIRES__: string;
