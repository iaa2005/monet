node_modules/

bun.lock

test_*

.env

*.log

.monet
.monet-prod
.monet-dev

# Reference clone of MoonshotAI/kimi-code — read-only, has its own .git.
# See info/kimi-code-audit.md.
kimi-code/

# Reference clone of itayinbarr/little-coder (Apache 2.0) — read-only.
# See info/little-coder-audit.md.
little-coder/

# The voice cloner is run in place during development; its cache of folded
# models and its downloaded speaker model must never reach the repo.
desktop/resources/voice-cloner/cache/
desktop/resources/voice-cloner/__pycache__/
desktop/resources/voice-cloner/*.onnx
desktop/resources/voice-cloner/*.wav
desktop/resources/voice-cloner/*.json
desktop/resources/voice-cloner/*.npz
